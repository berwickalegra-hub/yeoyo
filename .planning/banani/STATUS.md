# Banani implementation status — YeOyo Mariage

Last updated: 2026-08-01

Flow: `YeOyo Mariage` (Banani flow id `alMLvZczLcpt`) — a marriage-intent dating app for Kinshasa, DRC, Mobile Money only. The full screen-by-screen breakdown (structure, fields, interactive elements) lives in this session's analysis — re-fetch via `mcp__banani__banani_get_selected_designs` (screenIds below) when a screen plan file is created, since the raw export itself isn't committed to the repo.

See [IMPLEMENTATION-PLAN.md](./IMPLEMENTATION-PLAN.md) for the full gap analysis and phased roadmap — read that before picking up any screen below.

## Done
- [x] **Phase A — Foundation** (2026-07-30):
  - Design tokens: `frontend/src/app/globals.css` `@theme` block (dark palette, radii, DM Sans, type scale) from Banani's `/style.css`
  - Font swap: Inter → DM Sans in `frontend/src/app/layout.tsx` (`lang="fr"`)
  - `next.config.ts`: `images.remotePatterns` for `res.cloudinary.com` (profile photos)
  - Prisma migration `20260730052748_profile_and_photos`: `Profile` + `ProfilePhoto` models, `User.profile` + `FileUpload.profilePhoto` relations
  - `frontend/src/components/ui/Icon.tsx` — Lucide wrapper (extend per-screen as new icon names show up)
  - `frontend/src/components/ui/UserAvatar.tsx` — image-or-initials avatar primitive
  - `frontend/src/components/yeoyo/Sidebar.tsx` — canonical sidebar reconciling `SidebarNav.jsx`/`SidebarNavApp.jsx`, badge counts left at `undefined` (no live data yet) rather than Banani's mock numbers
  - `frontend/src/app/api/realtime/token/route.ts` — Ably token-mint route (`requireAuth`, 503 `REALTIME_NOT_CONFIGURED` if `ABLY_API_KEY` unset); capability is a `conversation:*` wildcard placeholder, tighten once Phase D's `Conversation` model lands
  - `.env.example`: added `ABLY_API_KEY`
  - Packages added: `ably`, `lucide-react`
  - Verified: `pnpm format && pnpm lint && pnpm typecheck && pnpm test` all green (571/571 tests)

- [x] **Phase B — Landing + Onboarding** (2026-07-30):
  - `frontend/src/app/page.tsx` — full replace, mobile-first responsive landing page merging Landing Mobile + Landing Desktop into one component (`lg:` breakpoint layers in the 2-col hero, 4-col feature grid, grouped footer)
  - New components: `frontend/src/components/yeoyo/{YeOyoNav,FeatureRow,StatChip,TestimonialCard,ProfileCardSmall,VerifiedBadge}.tsx`
  - `Icon.tsx` extended with search/shield-check/heart-handshake/smartphone/lock/gem/chevron-left/chevron-down/x/check/map-pin/clock/camera/info
  - `frontend/src/app/onboarding/page.tsx` — signup + verify-email (reusing existing protected auth routes as-is) + 4-step profile wizard (Profil de base/Ta situation/Ton intention/Ta photo), one step at a time at every breakpoint
  - `frontend/src/app/api/profile/route.ts` — new `POST`/`GET`, includes an 18+ age gate not present in the Banani mockup (legal necessity for a dating app)
  - **Flagged departures from the mockup** (see file comments + IMPLEMENTATION-PLAN.md): (1) fabricated stock-photo "recent profiles"/testimonials replaced with initials avatars — shipping fake photos as verified members is a trust problem; (2) desktop's all-steps-stacked-with-dimming onboarding chrome simplified to one-step-at-a-time everywhere, revisit as polish; (3) Banani's 3-dropdown DOB picker replaced with a native `<input type="date">`
  - Verified: `pnpm format && pnpm lint && pnpm typecheck && pnpm test` all green (572/572); dev-server smoke check — both `/` and `/onboarding` return HTTP 200, compiled CSS confirmed carrying the dark/gold theme (`#0d0d0d`, `#c9a84c`, DM Sans), no compile/hydration errors in the server log. **Not verified**: actual visual rendering at 375px/1280px in a real browser — no headless-browser tool (chromium-cli/Playwright) was available in this environment; recommend `/run-skill-generator` if visual UI checks will recur.

- [x] **Phase C — Découverte + Explorer** (2026-07-30):
  - Prisma migration `20260730061037_likes_and_contact_requests`: `Like` + `ContactRequest` models (liking auto-creates a PENDING `ContactRequest` in one transaction, per the Demandes screen's own explainer)
  - `frontend/src/lib/server/profile/card.ts` — shared `ageInYears`/`profileTags`/`toProfileCard` helpers (now used by 3 routes)
  - `frontend/src/lib/server/storage.ts` — added `cloudinaryUrlForKey()` (reconstructs a delivery URL from the `FileUpload.key` the upload route persists; not itself a protected file)
  - New routes: `POST /api/likes`, `GET /api/profiles/discover` (profile-of-the-day + compatibility score), `GET /api/profiles/explorer` (paginated/filtered grid)
  - New pages: `frontend/src/app/app/decouvrir/page.tsx`, `frontend/src/app/app/explorer/page.tsx`
  - New components: `ProfileDetailCard.tsx`, `ExplorerCard.tsx`, `ProfilePhotoCover.tsx` (real Cloudinary photo when present, initials fallback otherwise — these are REAL authenticated users now, unlike Landing's placeholder cards)
  - **Sidebar fix**: Phase A's reconciliation had dropped "Découvrir" as a distinct tab (merged into "Explorer") — since both screens are being built as separate routes, added `decouvrir` back as its own `SidebarTab`/nav item (first position, `layout-grid` icon, matching the original `SidebarNav.jsx`'s tab order)
  - **New infra not from Banani**: `/login` page — no login screen was ever exported from Banani (only first-time signup was designed), but `useUser()`'s redirect target must exist for any authenticated page to work. Built functionally, styled with the app theme, no Banani reference to match pixel-for-pixel.
  - **Scope decisions flagged in code comments**: "Dislike" (X) is client-side only in v1, no persisted pass/skip; Explorer's gender chips allow explicit override of the discover route's opposite-gender default; job/interests/languages fields aren't collected anywhere in the onboarding wizard despite appearing on cards (Banani's own mockup gap) — tags are built only from religion + childrenCount.
  - Verified: `pnpm format && pnpm lint && pnpm typecheck && pnpm test` all green (575/575); dev-server smoke check — `/app/decouvrir`, `/app/explorer`, `/login` return HTTP 200; `/api/profiles/discover`, `/api/profiles/explorer` return 401 unauthenticated (not 500); `/api/likes` returns 403 without CSRF (not 500); no compile/runtime errors in the server log. Visual/breakpoint rendering still not verified in a real browser (same tooling gap as Phase B).

- [x] **Phase D — Demandes + Messages** (2026-07-30):
  - Prisma migration `20260730070432_conversations_messages_blocks`: `Conversation` (canonical sorted userAId/userBId pair, `@@unique([userAId, userBId])`, 1:1 with the accepted `ContactRequest`), `Message`, `BlockedUser`
  - `frontend/src/lib/server/conversations/lib.ts` — `orderedPair`/`isParticipant`/`otherParticipant` shared helpers
  - `frontend/src/lib/server/blocks.ts` — `isBlockedEitherWay`/`blockedUserIds`, wired into `POST /api/likes` (404, not 403, mirroring the org-membership non-leak pattern) and both discovery routes (excluded from candidate pools)
  - New routes: `GET /api/contact-requests` (Demandes inbox, `?type=received|sent`), `POST /api/contact-requests/[id]/respond` (ACCEPT creates the `Conversation` in the same tx as the status flip; DECLINE just cancels), `GET /api/conversations` (inbox list + last message + unread count via `groupBy`), `GET/POST /api/conversations/[id]/messages` (thread page + send; GET marks the other side's messages read as a side effect), `POST`/`DELETE /api/users/[id]/block`, `GET /api/app/nav-counts` (sidebar badge counts — the backend Phase A's Sidebar comment said would "land in Phase C/D")
  - `POST /api/realtime/token` (Phase A scaffold) **tightened**: capability is now built per-caller from their actual `Conversation` rows (`conversation:${id}`), replacing the `conversation:*` wildcard placeholder
  - Real-time: `POST /api/conversations/[id]/messages` publishes to the `conversation:{id}` Ably channel via `Ably.Rest` after the DB write (best-effort — a publish failure is logged, not thrown, so message delivery never depends on Ably being up); inert (skipped) when `ABLY_API_KEY` is unset, consistent with the rest of the optional-provider pattern
  - Notification: `messageReceived()` added to `notifications/templates.ts`, dedupeKey = the Message's own id (each row is one logical event, not time-based)
  - New components: `ContactRequestCard.tsx`, `ConversationListItem.tsx`; new hooks: `lib/yeoyo/useConversations.ts`, `lib/yeoyo/useNavCounts.ts` (also wired into Découverte/Explorer's Sidebar so badge counts are consistent across every nav-bearing page, not just the two new ones)
  - New pages: `frontend/src/app/app/demandes/page.tsx` (Reçues/Envoyées tabs, accept routes straight into the new conversation), `frontend/src/app/app/messages/page.tsx` (inbox list + empty-state right pane on desktop), `frontend/src/app/app/messages/[id]/page.tsx` (two-pane thread: conversation list + active thread on `lg:`, full-screen thread with a back chevron on mobile) — thread page subscribes to Ably client-side (`authUrl: '/api/realtime/token'`), dedupes the send-response message against the Ably echo by message id since the publish always reaches every subscriber including the sender's own tab
  - **Scope decisions flagged in code**: blocking hides both users from discovery + refuses new messages but keeps existing conversation/message rows (moderation history, not silent deletion); `/api/app/nav-counts` is fetched once per page mount, not itself Ably-driven (a new message only updates the badge on next navigation/refresh — acceptable for a sidebar badge, unlike the open thread which is live)
  - Verified: `pnpm format && pnpm lint && pnpm typecheck && pnpm test` all green (581/581); dev-server smoke check — `/app/demandes`, `/app/messages`, `/app/messages/[id]` return HTTP 200; all new API routes return 401 (GET, no session) / 403 (POST, no CSRF) — never 500 — against an unauthenticated dev server; no compile/runtime errors in the server log. Visual/breakpoint rendering still not verified in a real browser (same tooling gap as Phases B/C).

- [x] **Phase E — Paramètres + Premium Checkout** (2026-07-30):
  - Prisma migration `20260730072443_subscriptions`: `Subscription` model (`planId` references the static catalog below, `provider` = `"stub"` for now, `orderId` unique 1:1 with the `Order` that (would) activate it)
  - `frontend/src/lib/server/subscriptions/plans.ts` — static 3-plan catalog (Mensuel/6 Mois/Annuel), not a DB table (no admin-editable-plans requirement in the mockup). **Flagged pricing decision**: Banani prices in "FC" (2 500/12 500/20 000) but IMPLEMENTATION-PLAN.md §4 says USD is the real charge currency (Moneroo's DRC/CDF settlement is still unconfirmed) — the USD cents in `plans.ts` are a placeholder chosen to match the mockup's discount shape (~25% off for 6-month, ~40% for annual), not a real pricing decision. CDF is shown display-only via an illustrative, non-live conversion rate.
  - `frontend/src/lib/server/payments/stub.ts` — **fake payment provider, per explicit user decision**: build the checkout UI end-to-end now, wire a real Stripe/Moneroo charge later. `createStubCharge()` never touches money; it hands back an internal `/app/premium/pending` URL instead of a real hosted-checkout redirect.
  - New routes: `GET /api/subscriptions/plans`, `GET /api/subscriptions/me`, `POST /api/subscriptions/checkout` (creates Order+Subscription in one tx; state-based idempotency, not header-based — a stub charge can't double-bill so the full Stripe-grade Idempotency-Key/CircuitBreaker machinery from `POST /api/orders` wasn't needed, but should be adopted when a real provider replaces the stub), `GET /api/subscriptions/orders/[id]` (polled by the pending page), `POST /api/subscriptions/orders/[id]/simulate-payment` (**stand-in for a real Stripe/Moneroo webhook** — marks Order PAID + Subscription ACTIVE; delete this route once real billing lands, nothing else in the flow needs to change since the pending page already polls status rather than assuming any particular completion mechanism)
  - `PATCH /api/profile` (new) — Paramètres' "Profil" (visibility/online-status toggles) and "Préférences de recherche" (commune/intent) sections edit the same `Profile` fields discovery already filters on, rather than a separate `searchPrefs` blob
  - `GET /api/users/blocked` (new) — Paramètres "Utilisateurs bloqués" list (unblock itself reuses Phase D's `DELETE /api/users/[id]/block`)
  - `GET /api/account/export` (new) — Paramètres "Télécharger mes données", JSON dump of the caller's own rows only, `passwordHash` excluded
  - `DELETE /api/account` (new) — Paramètres "Supprimer mon compte": password-holding accounts confirm with their current password (`verifyPassword` from the protected `auth.ts`, not reimplemented); OAuth-only accounts (no password) re-type their own email instead. Cascades via existing Prisma relations; clears auth+CSRF cookies after deletion.
  - **Notification wiring closed a gap from Phase D**: `contactRequestReceived()` added to `templates.ts` and wired into `POST /api/likes` (dedupeKey = the `ContactRequest`'s own id, so re-liking doesn't re-notify) — Paramètres' "Demandes de contact" toggle would otherwise have controlled a notification that never fired. Both this and the existing `messageReceived()` call site now check `isChannelEnabled()` against the recipient's `NotificationPreferences` before calling `createNotification` — previously neither respected the toggle at all. Extracted a shared `parsePrefs()` helper in `prefs-merge.ts` (was a private function duplicated in the prefs route) so both call sites and the route share one JSON-narrowing implementation. **"Likes reçus" and "Profils recommandés" toggles remain inert** — no notification is fired for either yet (likes already imply a contact-request notification; "recommended profiles" has no natural trigger point, it would need a cron/algorithmic job — out of scope here).
  - New components: `Toggle.tsx` (switch control), `SettingsSection.tsx`/`SettingsRow.tsx` (card + row wrappers, used 7 times); new shared constant module `lib/yeoyo/constants.ts` (`KINSHASA_COMMUNES`, `INTENT_OPTIONS`) extracted out of `onboarding/page.tsx` so Paramètres' search-prefs selectors don't duplicate the same option lists
  - New pages: `frontend/src/app/app/parametres/page.tsx` (all 7 Banani sections: Compte/Profil/Notifications/Préférences de recherche/Paiement/Données et confidentialité/À propos, plus the Utilisateurs-bloqués row and Se-déconnecter footer), `frontend/src/app/app/premium/page.tsx` (3 plan cards + order summary + payment-method selector + comparison table; shows an "already Premium" state instead of the picker when `GET /api/subscriptions/me` returns ACTIVE), `frontend/src/app/app/premium/pending/page.tsx` (not a Banani screen — stands in for whatever a real hosted-checkout redirect would show; polls Order/Subscription status, has a clearly-labeled "Simuler la confirmation du paiement" demo button)
  - **Flagged departures from the mockup**: no "Numéro de téléphone" field — neither `User` nor `Profile` has a phone column, and adding one is a bigger auth-boundary decision (verification, uniqueness) than a settings-page edit belongs to; left out rather than faked. "Méthodes de paiement" (saved M-Pesa/Airtel/Orange entries) isn't a persisted concept — there's no real provider tokenizing a method to save yet — replaced with a link into Premium Checkout plus a plain payment history list built from the user's own `Order` rows.
  - Verified: `pnpm format && pnpm lint && pnpm typecheck && pnpm test` all green (589/589); dev-server smoke check — `/app/parametres`, `/app/premium`, `/app/premium/pending` return HTTP 200; all new API routes return 401 (GET, no session) / 403 (POST/PATCH/DELETE, no CSRF) — never 500; no compile/runtime errors in the server log. Visual/breakpoint rendering still not verified in a real browser (same tooling gap as Phases B/C/D).

- [x] **Phase F — Admin Panel** (2026-07-30) — **A→F roadmap complete**:
  - Prisma migration `20260730075758_reports_and_verification_status`: `Report` model (reporterId/targetId/reason/status/resolvedAt) + `Profile.verificationStatus` (`PENDING | VERIFIED | REJECTED`, drives the Vérification IA queue; `verifiedAt` is still only set together with `VERIFIED`, unchanged consumer contract in `profile/card.ts`)
  - **User-facing gap closed**: no Banani screen ever exposed a "Signaler" (report) trigger — only the Admin Panel's review side was designed — so a moderation queue with nothing feeding it would've been dead UI. Added "Signaler" + "Bloquer" buttons to the Messages thread header (`/app/messages/[id]`) — the Messages screen's own spec listed "Bloquer" as a right-panel action that Phase D never actually wired to a button, so this closes two gaps at once.
  - New routes: `POST /api/reports` (user-facing), `GET /api/admin/reports` (cursor pagination + `?status=`, defaults to PENDING, now also returns `total` for sidebar badges), `POST /api/admin/reports/[id]/resolve` (RESOLVE/DISMISS, audit-logged, idempotent no-op on already-resolved), `GET /api/admin/verification-queue` (oldest-first, `onboardingCompletedAt not null` + `verificationStatus=PENDING`), `POST /api/admin/verification-queue/[id]/process` (APPROVE sets `verifiedAt`+`VERIFIED`, REJECT sets `REJECTED`, audit-logged), `GET /api/admin/stats/overview` (4 KPIs + signups-by-month via `$queryRaw` `date_trunc` + a 4-bucket member breakdown)
  - **Member breakdown mapping decision**: Banani's donut (Actifs/Vérifiés/En attente/Inactifs, sums to 100%) needs 4 mutually-exclusive buckets from data this kit already has — mapped "Vérifiés"/"En attente" to `Profile.verificationStatus`, "Inactifs" to `User.status === 'SUSPENDED'` (closest existing concept, this kit has no separate inactive-user state), "Actifs" = everyone else. Revenue KPI reads `Order.amount` where `status='PAID'` — currently $0 until real Stripe/Moneroo billing lands (Phase E's stub provider), which is accurate, not a bug.
  - New components: `AdminSidebar.tsx` (reconciled from the Banani `AdminSidebar.jsx` nav tree — Dashboard/Utilisateurs→Membres,Vérification IA,Signalements/Activité/Finance/Système; only the 4 built routes are clickable, the rest render as inert "Bientôt" labels rather than dead links)
  - **Admin theme colors — resolved by falling back, not re-fetching**: the `--color-admin-*` tokens referenced throughout the Banani export were never actually defined in either fetch, and re-fetching requires the user's own Banani MCP session (not available to drive autonomously). Per the standing fallback noted in this file, `AdminSidebar`/`/admin/*` reuse the main app's existing `--color-*` dark/gold tokens instead of inventing new ones.
  - New pages: `frontend/src/app/admin/layout.tsx` (gates on `GET /api/admin/me`, mirrors `examples/frontend-pages/admin/layout.tsx`'s pattern restyled dark/gold), `frontend/src/app/admin/page.tsx` (Dashboard: KPI row, CSS bar chart + CSS conic-gradient donut — matching the mockup's own from-scratch chart approach rather than pulling in a chart library — Membres récents/Signalements/Vérification IA panel slices, Activité récente feed reusing the existing `AdminAction` audit log), `frontend/src/app/admin/membres/page.tsx` (reuses `/api/admin/users` + `/api/admin/users/[id]/status` near-verbatim — the least new backend of the whole Admin Panel, as IMPLEMENTATION-PLAN.md §6 predicted), `frontend/src/app/admin/verification/page.tsx`, `frontend/src/app/admin/signalements/page.tsx`
  - **Scope narrowed, flagged**: only Dashboard/Membres/Vérification IA/Signalements were built — Suspensions, Likes & Matches, Messages, Demandes, Abonnements, Transactions, Configuration, and Logs are sidebar items Banani's own nav "implies" exist but were never actually exported as screens; building them wasn't asked for in IMPLEMENTATION-PLAN.md §6 either (which only scoped stats/Report/verification-queue routes + reusing `/api/admin/users`).
  - Verified: `pnpm format && pnpm lint && pnpm typecheck && pnpm test` all green (595/595); dev-server smoke check — `/admin`, `/admin/membres`, `/admin/verification`, `/admin/signalements` return HTTP 200; all new API routes return 401 (GET, no session) / 403 (POST, no CSRF) — never 500; no compile/runtime errors in the server log. Visual/breakpoint rendering still not verified in a real browser (same tooling gap as every phase since B).

- [x] **Responsive pass — mobile/tablet/desktop** (2026-08-01):
  - **Re-fetched the currently-selected Banani designs to check for drift/updates before starting** (per user's ask to compare against what's built): identical 11 screens as prior fetches, no new/changed screens, no new shared components. Confirmed via regex scan: **zero `sm:`/`md:`/`lg:`/`xl:` responsive prefixes anywhere in the entire export** — every screen is a single fixed-breakpoint artboard (8 desktop-only, 2 mobile-only, 1 desktop-flow), and `SidebarNav.jsx`/`SidebarNavApp.jsx`/`AdminSidebar.jsx` are all fixed-width (`w-60`/`w-56`), always-visible, with no mobile/tablet treatment of any kind. Banani supplied no responsive logic to crib from — this pass is net-new work layered on top of the Banani-sourced desktop components, not a translation of an existing responsive mockup.
  - **The one real bug found**: every `/app/*` page rendered `Sidebar` as a permanent `w-60` (240px) fixed-width column with **zero mobile handling** — on a 375px phone, that's 240px of permanently-visible sidebar squeezing content into 135px. This was the one structural gap; page-level content (grids, cards) already had reasonable `sm:`/`lg:` coverage from earlier phases.
  - `frontend/src/components/yeoyo/nav-items.ts` (new) — `NAV_ITEMS`/`MOBILE_TAB_IDS`/`badgeFor()` extracted out of `Sidebar.tsx` so the sidebar and the new bottom tab bar share one source of truth for nav order/hrefs/badges.
  - `Sidebar.tsx` rewritten responsive: **hidden below `md`** (replaced by `MobileTabBar`), an **icon-only rail at `md`** (labels/user-name/upsell panel hidden, small dot badge instead of a numbered pill), the **full labeled sidebar from `lg` up** (unchanged visually from before). Also dropped the Banani export's leftover `minHeight: '900px'` inline style — an artboard-canvas artifact; the flex parent's `min-h-screen` already stretches it without a hardcoded pixel height.
  - `frontend/src/components/yeoyo/MobileTabBar.tsx` (new) — fixed bottom tab bar, 5 items (Découvrir/Explorer/Demandes/Messages/Paramètres), `md:hidden`. **Not shown on `/app/messages/[id]`** (the thread view) — a persistent tab bar competing with the message input for thumb space is bad chat UX; the thread's own back-chevron already provides mobile navigation back to the inbox.
  - `frontend/src/components/yeoyo/AppShell.tsx` (new) — the shared `Sidebar` + content-slot + `MobileTabBar` wrapper; `pb-16 md:pb-0` on the content slot keeps scrollable content from sitting under the fixed bottom bar. Refactored `/app/decouvrir`, `/app/explorer`, `/app/demandes`, `/app/parametres`, `/app/premium`, and `/app/messages` (inbox) to use it — each was a mechanical wrapper swap, no content changes. `/app/messages/[id]` deliberately still renders `Sidebar` directly (no `AppShell`, no bottom bar, per the above).
  - `frontend/src/components/admin/AdminSidebar.tsx` — same fixed-sidebar problem, different fix: admin nav items are text-only (no icons in the Banani source), so an icon rail like the consumer `Sidebar` wasn't practical without inventing icons for every item. Instead: **permanent sidebar from `lg` up, an off-canvas drawer + backdrop below `lg`**, toggled by a hamburger button (new `menu` icon added to `Icon.tsx`) in `admin/layout.tsx`'s top bar — the standard pattern for a desktop-first admin tool that still shouldn't be unusable on a tablet/phone.
  - Tablet (`md:`) polish: `decouvrir`'s profile+compatibility grid now 2-up from `md` (was `lg`); Admin Dashboard's chart/panel rows now 2-up from `md` (was `lg`); `/admin/membres`'s title+search header now stacks only below `sm` instead of always cramming a search form next to the title; the search input itself gained `min-w-0`/`w-full sm:w-auto` so it can't force horizontal overflow on a narrow screen; `/admin/layout.tsx`'s content padding is now `p-4 md:p-6 lg:p-8` instead of a flat `p-8` that ate 64px of a 375px viewport.
  - **Landing/Onboarding/Login left as-is, deliberately**: onboarding's wizard card (`max-w-md lg:max-w-lg`, centered) already scales gracefully across all three breakpoints without changes. The landing page's marketing sections (testimonials, "how it works", "why YeOyo") stack single-column from mobile through tablet and only switch to a grid at `lg` — a legitimate, common pattern for content sections (not a bug), left alone rather than adding a third breakpoint state to already-intricate `lg:block`/`lg:grid` conditional markup for marginal tablet gain.
  - Verified: `pnpm format && pnpm lint && pnpm typecheck && pnpm test` all green (595/595, no route/test count change — this was a pure UI/component refactor); dev-server smoke check — all 10 representative routes (`/`, `/login`, `/onboarding`, all 7 `/app/*` pages, `/admin`) return HTTP 200, no compile/runtime errors in the server log; fetched the compiled CSS bundle directly and confirmed the `@media (min-width: 48rem)` (`md`) and `@media (min-width: 64rem)` (`lg`) blocks exist and contain the exact utility classes this pass introduced (`w-20`, `w-60`, `-translate-x-full`, `translate-x-0`, drawer/bottom-bar positioning classes) — as close to a responsive-behavior check as is possible without a headless browser. **Not verified**: actual visual rendering/touch-target sizing at real 375px/768px/1280px viewports in a real browser — no headless-browser tool (chromium-cli/Playwright) was available in this environment, same disclosed gap as every phase since B.

## In progress
_(none — all 6 phases of the confirmed A→F roadmap are built, plus a full responsive-design pass. Remaining work is explicitly deferred, not scoped: real Stripe/Moneroo billing (currently a stub provider), Moneroo DRC/CDF settlement confirmation, a real AI-verification vendor, Resend email configuration, and real-browser visual/breakpoint QA — see "Open design questions" below for each.)_

- [x] **Découverte Profils — re-fetch correction** (2026-08-07): the original Phase C build only reproduced the right "Profil du jour" panel; the real Banani screen is a full grid + filter bar + right panel. Rebuilt `frontend/src/app/app/decouvrir/page.tsx` from scratch to match. Plan: `.planning/banani/decouverte-profils-v2.md`. CTA text kept as "Envoyer un message" (opens chat) rather than Banani's "Voir le profil" — no full profile-view page exists, and message-first was just tested end-to-end; user explicitly approved this divergence.
- [x] **4-screen design-fidelity pass — Paramètres/Explorer/Demandes/Messages** (2026-08-07): re-fetched all 4 currently-selected Banani screens and corrected drift:
  - `parametres/page.tsx`: added missing header subtitle, widened content column to `lg:max-w-3xl` (was `max-w-2xl`)
  - `components/yeoyo/ExplorerCard.tsx`: job line now shows `{job} · {religion tag}` matching Banani's `job · faith` format (was job-only)
  - `demandes/page.tsx`: added the 3rd tab ("Contacts" — merged ACCEPTED requests from both directions, deduped) and the right "Comment ça marche" explainer panel (3 steps + Premium upsell), both missing from the Phase D build
  - `messages/[id]/page.tsx`: added the right profile-info panel (avatar/name/à propos/tags + "Ajouter un like"/"Bloquer" actions), `lg:` only — missing from the Phase D build. "Voir le profil complet" deliberately omitted (no full profile page exists; same reasoning as Découverte above)
  - `components/yeoyo/ContactRequestCard.tsx`: added optional `conversationId` prop — ACCEPTED rows now show a real "Message" button instead of a static status pill
  - `components/ui/Icon.tsx`: added `bell`, `check-circle`, `inbox`, `ban`
  - Verified: `pnpm format && pnpm lint && pnpm typecheck && pnpm test` all green (597/597); dev-server smoke check on all 4 routes + live curl against `/api/contact-requests?type=received|sent`. Visual/breakpoint rendering still not verified in a real browser (same disclosed tooling gap as every phase).

## Pending (seen in Banani, fetched twice, not yet per-screen planned)

_(none — every exported screen has a corresponding implementation phase above)_

## Open design questions

- ~~Payment provider~~ — **RESOLVED 2026-07-30**: Stripe (cards, USD) + Moneroo (Mobile Money), replacing Bictorys. See IMPLEMENTATION-PLAN.md §4.
- ~~Real payment wiring~~ — **RESOLVED 2026-07-30**: build Premium Checkout UI against a stub/fake `PaymentProvider` now; wire a real Stripe/Moneroo charge later (separate task, not blocking Phase E's UI). Moneroo DRC/CDF verification still needed before THAT later step.
- ~~Onboarding steps 2-4~~ — **RESOLVED 2026-07-30**: full 4-step flow now in `onboarding-flow` above.
- Mobile variants only exist for Landing; the rest (including the new Admin Panel) are desktop-only in the export — per skill rules we still build mobile-first, but there's no Banani mobile mockup to match pixel-for-pixel for those screens. Raised 2026-07-30, still open.
- Two divergent sidebar components (`SidebarNav.jsx` vs `SidebarNavApp.jsx`) need reconciling into one canonical nav — raised 2026-07-30, still open (Phase A).
- ~~AI profile verification~~ — **RESOLVED 2026-07-30**: no vendor yet, defer — ship as admin-manual-approval via the Admin Panel's "Vérification IA" queue for now; Claude will propose a vision-AI vendor when this is picked back up.
- ~~Real-time messaging~~ — **RESOLVED 2026-07-30**: real-time from day one via Ably (user decision), not deferred. See IMPLEMENTATION-PLAN.md §5.
- ~~Admin theme colors~~ — **RESOLVED 2026-07-30 (Phase F)**: fell back to reusing the main app's dark/gold `--color-*` tokens rather than re-fetching from Banani (that requires the user's own MCP session, unavailable for an autonomous continuation) — see Phase F's AdminSidebar note.
- **Order confirmed 2026-07-30**: build in the roadmap's A→F order as proposed (foundations → landing/onboarding → discovery → messages → payment UI → admin). **All 6 phases now complete.**
- **New, raised 2026-07-30 (Phase E)**: `lib/server/subscriptions/plans.ts`'s USD prices are a placeholder (chosen to match the mockup's discount percentages, not a real pricing decision) — finalize real USD pricing at the same time the Moneroo DRC/CDF settlement question above gets resolved, since both block real billing together.

### Remaining work, not scoped by any phase (raise with the user before starting)
1. **Real Stripe/Moneroo billing** — Premium Checkout runs against `lib/server/payments/stub.ts`; wiring a real charge means: confirm Moneroo's DRC/CDF settlement (item above), finalize real USD pricing, implement the Stripe + Moneroo adapters per the `izisaas-payments-handler` skill, delete `POST /api/subscriptions/orders/[id]/simulate-payment`, add real webhook handling.
2. **AI profile verification vendor** — Vérification IA is admin-manual-approval only; propose a vision-AI vendor when picked up (per the user's original ask).
3. **Resend email configuration** — verification-code emails still don't arrive in real inboxes (`RESEND_API_KEY`/`EMAIL_FROM` unset in `.env.local`); the outbox row is created but never drained. User explicitly deferred this after Phase B.
4. **Real-browser visual/breakpoint QA** — every phase's dev-server smoke check confirms HTTP 200 + no compile errors, never actual pixel rendering at 375px/1280px — no headless-browser tool was available in this environment across all 6 phases.
